@charset "utf-8";
/* CSS Document */

body {
	font: 100%/1.4 Verdana, Arial, Helvetica, sans-serif;
	background: #E7E6E6;
	margin: 0;
	padding: 0;
	color: #000;
}

/* element/tag selectors */
ul, ol, dl { 
	padding: 0;
	margin: 0;
}
h1, h2, h3, h4, h5, h6, p {
	margin-top: 0;	
	padding-right: 15px;
	padding-left: 15px; 
}
a img {
	border: none;
}


a:link {
	color: #000;
	text-decoration: underline; 
}
a:visited {
	color: #FFF;
	text-decoration: underline;
}
a:hover, a:active, a:focus { 
	text-decoration: none;
}

/* this fixed width container surrounds the other divs */
.container {
	width: 960px;
	background: #FFF;
	margin: 0 auto;
}

.header {
	background: #E7E6E6;
}
.userinfo {
	background: #E7E6E6;
	text-align:right;
}
.sidebar {
	float: left;
	width: 180px;
	
	padding-bottom: 10px;
}
.content {

	padding: 10px 0;
	width: 780px;
	float: left;
}


.content ul, .content ol { 
	padding: 0 15px 15px 40px; 
}

ul.nav {
	list-style: none; /* this removes the list marker */
	border-top: 1px solid #FFF; 
	margin-bottom: 15px; 
}
ul.nav li {
	border-bottom: 1px solid #FFF; /* this creates the button separation */
}
ul.nav a, ul.nav a:visited { /* make sure links retain their button look even after being visited */
	padding: 5px 5px 5px 15px;
	display: block;
	width: 160px;
	text-decoration: none;
	background: #5B9BD5;
}
ul.nav a:hover, ul.nav a:active, ul.nav a:focus {
	background: #9DC3E6;
	color: #FFF;
}

/* the footer */
.footer {
	padding: 10px 0;
	background: #D4D2D2;
	position: relative;
	clear: both; 
}

/* miscellaneous float classes (useful utilities for laying out a page) */
.floatright { 
	float: right;
	margin-left: 8px;
}
.floatleft {
	float: left;
	margin-right: 8px;
}